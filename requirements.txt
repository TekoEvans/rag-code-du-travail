# Briques élémentaires autorisées (pas de framework RAG type LangChain/LlamaIndex)
sentence-transformers>=3.0.0   # modèle d'embedding multilingue
chromadb>=0.5.0                # base vectorielle persistée + métadonnées
groq>=0.11.0                   # appels LLM (réponse, modérateur, HyDE)
python-dotenv>=1.0.0           # chargement des variables d'environnement (.env)
